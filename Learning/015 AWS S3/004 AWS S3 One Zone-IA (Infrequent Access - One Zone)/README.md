# AWS S3 One Zone-IA (Infrequent Access - One Zone)

 Like S3 Standard-IA, but data is stored in only one Availability Zone (AZ), instead of being replicated across multiple AZs. Suitable for infrequently accessed data that doesn’t require multi-AZ resilience.
